# Rust build artifacts
/target
/dist

# Build logs
*.log

# Local data (keys/token created at runtime)
# all_keys.json is the map shape the real-db tests accept; ignored so a copy
# dropped in the repo root can never be committed.
keys.json
all_keys.json

# IDE / editor
.idea/
.vscode/
*.iml

# OS noise
Thumbs.db
Desktop.ini
.DS_Store

# Local overrides
*.local
.env

# Config files
qqflow-server.json

# Editor/tool leftovers
*.bak
*.json.tmp
